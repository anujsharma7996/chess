class ChessPiece {
  constructor(name, id, isAttacked, color) {
    this.name = name;
    this.id = id;
    this.isAttacked = isAttacked;
    this.color = color;
  }

  setSquare(square) {}

  getSquare() {}
}
